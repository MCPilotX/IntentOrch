/**
 * Planner — Unused stub.
 *
 * This class was intended to extract plan-generation logic from CloudIntentEngine,
 * but the refactoring was never completed. CloudIntentEngine.planQuery() continues to
 * handle all planning logic directly via LLM function calling.
 *
 * @deprecated CloudIntentEngine.planQuery() is the sole plan-generation entry point.
 *             Remove this module in the next major version.
 */
export {}
